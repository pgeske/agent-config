---
description: Write an assigned worker checkpoint or accepted closeout; stay open
argument-hint: "<report path, assignment, and optional acceptance status>"
---

Load and follow the `worker-handoff` skill. Default to a checkpoint unless the
coordinator/user explicitly accepted this assignment. Ask for missing assignment
or report-path information. Leave the session open.
Assignment guidance:

$ARGUMENTS
