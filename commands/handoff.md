---
description: Write a self-contained handoff note for the current session
argument-hint: "[topic, focus, or next task]"
---

Create a durable handoff note for the current session now.

Load and follow the `session-handoff` skill. Treat the following as optional user guidance for the handoff's topic, focus, or intended next task:

$ARGUMENTS

If no guidance was supplied, infer the main active work from the session. Write the note, then report its exact path, current-state summary, and first next actions. Do not end or switch the session.
