---
description: Sync portable work agent setup changes into the personal pgeske setup
argument-hint: "[focus or additional constraints]"
---

Sync the work agent configuration into the personal agent configuration now.

Load and follow the `sync-agent-config` skill. Include committed and local uncommitted source changes, preserve deliberate personal changes, reconcile competing implementations by quality, and exclude employer-specific configuration, internal services, Slack integrations, and all secrets.

Treat the following as optional focus or constraints:

$ARGUMENTS
