---
description: Refresh coordinator memory and collect available worker checkpoints
argument-hint: "[scope or focus]"
---

Load and follow the `coordinator-memory` skill in wrap-up mode.
Update the daily log and living briefing. Request reports only from this
coordinator's known workers when safely reachable; record unavailable updates
without blocking the whole wrap-up. Do not spawn workers, interrupt active work,
accept unfinished work, or close sessions.
Optional scope:

$ARGUMENTS
